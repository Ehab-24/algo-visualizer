
import 'dart:math';

import 'package:algo_visualizer/UI/PathfindingScreen/SideBarPathfinding.dart';
import 'package:algo_visualizer/UI/PathfindingScreen/GridVisualizer.dart';
import 'package:flutter/material.dart';

import '../../Globals/constants.dart';
import '../../Globals/functions.dart';

class ScreenPathfinding extends StatelessWidget {
  const ScreenPathfinding({super.key});

  @override
  Widget build(BuildContext context) {

    final w = screenWidth(context);

    return Scaffold(

      backgroundColor: background,

      body: Row(
        children: [

          
        ],
      ),
    );
  }
}